# Discord.py library
# https://github.com/Rapptz/discord.py
discord.py[voice] @ git+https://github.com/Rapptz/discord.py

# Used to color output logs.
colorama

# Database
pony

# Used to normalize audio levels and combine audio files.
pydub

# Piper TTS for local text-to-speech
piper-tts

# ElevenLabs TTS API
elevenlabs


# PIL
pillow

# Dota 2 database
# Pinned so the quiz's game data version is explicit: 7.8.12 carries patch 7.41
# data (facets removed, nine new items). Bump deliberately after a game patch.
dotabase==7.8.12
sqlalchemy_utils

# OpenDota API
opendota2py

# Youtube-dl
# Pinned so the version is explicit in git and a bump actually invalidates the
# Docker pip layer. YouTube rotates its player/extractor constantly, so a stale
# yt-dlp fails as googlevideo 403s at playback time (ffmpeg gets a URL issued
# for a client YouTube has since blocked) rather than as an extraction error.
# Bump this when playback starts 403ing.
yt-dlp==2026.8.19

# Youtube API
python-youtube

# Spotify API
spotipy

# Lyrics Genius API
lyricsgenius

# Development tools
pre-commit
isort
yamllint


# Fish TTS dependencies (via plomtts server)
plomtts-client @ git+https://github.com/plomdawg/plomtts.git#subdirectory=client

fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
jinja2==3.1.2
pydantic==2.4.2
